services:
  app:
    image: tvwatchlist-app:local
    build:
      context: .
      dockerfile: ./docker/php/Dockerfile.dev
    container_name: watchlist-app
    environment:
      OPCACHE_VALIDATE_TIMESTAMPS: 0
      ENV: dev
      XDEBUG_MODE: "off"
    volumes:
      - ./app:/var/www/html:rw,cached
    networks:
      - app-network
    expose:
      - "9000"
    # Ensure php-fpm is the command
    command: ["php-fpm", "-F", "-R"]

  nginx:
    container_name: tvwatchlist-nginx
    image: nginx:stable-alpine
    ports:
      - '${APP_PORT}:80'
    volumes:
      - ./app/public:/var/www/html/public
      - ./docker/nginx/conf.d/default.dev.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - app-network

  database:
    container_name: database
    image: mongo:7
    environment:
      MONGO_INITDB_DATABASE: api
      MONGO_INITDB_ROOT_USERNAME: api
      MONGO_INITDB_ROOT_PASSWORD: password
    volumes:
      - db_data:/var/lib/mongodb/data:rw
    networks:
      - app-network

volumes:
  db_data: {}

networks:
  app-network:
    driver: bridge